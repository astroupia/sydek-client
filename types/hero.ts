export interface HeroSectionProps {
    title: string;
    subtitle: string;
    gradientClassName?: string; // Optional, defaults to "text-gradient"
    highlightedWord?: string; // Optional, if provided, highlights this word
  }